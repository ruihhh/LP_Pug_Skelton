version: "3"
services:
  node:
    image: node:latest
    ports:
      - '8080:3000'
    volumes:
      - .:/var/www/html
    working_dir: /var/www/html
    command: /bin/sh -c "yarn install --dev && yarn run dev"
